# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'xctool-test', :test_paths => 'NanoStoreTests' do
  watch(%r{NanoStore/(.+)\.(m|mm)$})
  watch(%r{NanoStoreTests/(.+)\.(m|mm)$})
end
